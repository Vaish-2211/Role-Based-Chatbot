TO READ:
    sentence-transformers
    bm25
    ragas
    datasets
    pytest
    pytest-async
    slow-api
    lanchain core (complete)
**FastAPI** -   *It is used to create REST APIS in python*
**Uvicorn** -   *Creates and runs the http server, listens to port, handles requests and pass it to FASTAPI*
**Python-multipart** - *It is used to accept multipart form data, files to u r requests*
**python-jose[cryptography***]  - JSON Object String and Encryption*
**passlib[bcrypt]**  -  Used for hasing the passwords*
**pydantic-settings** - *centralize the configuration of all the keys, reads envs and set defaults and help in managing the environment variables*
**logguru** - *Used for logging*
**qdrant-client** - *connecting and working with Quadrant Database*
**Streamlit** - Building UI
**Groq**      - LLM
**httpx**     - http client ( sends request from UI )
